config:
  target: 'Replace this with your URL'
  phases:
    - duration: 1200
      arrivalRate: 5
      name: CodeGuru Profiler test
scenarios:
    - flow:
        - get:
            url: "/lambda?rank={{ $randomNumber(1, 570) }}"
